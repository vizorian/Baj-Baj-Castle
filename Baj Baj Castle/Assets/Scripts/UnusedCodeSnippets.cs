using System.Collections;
using UnityEngine;

namespace Assets
{
    public class UnusedCodeSnippets
    {
        /// <summary>
        /// Use when no items are equipped for directional walking sprites
        /// </summary>
        //// Checking if the player has moved in another direction
        //if (x != moveDelta.x)
        //{
        //    changedDirection = true;
        //}
        //else
        //{
        //    changedDirection = false;
        //}

        //// Flipping the sprite based on the direction of movement
        //if (x < 0 && changedDirection)
        //{
        //    _spriteRenderer.flipX = true;
        //}
        //else if (x > 0 && changedDirection)
        //{
        //    _spriteRenderer.flipX = false;
        //}
    }
}